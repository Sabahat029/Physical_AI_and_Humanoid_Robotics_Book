import { renderGradient } from './gradient.js';
export function renderGradients(gradients, variableName) {
    return gradients.flatMap((v, i) => {
        const gradient = renderGradient(v);
        if (!gradient)
            return [];
        return [
            {
                value: gradient,
                variableName: `${variableName}${i}`,
                type: 'gradient',
            },
        ];
    });
}
//# sourceMappingURL=gradients.js.map