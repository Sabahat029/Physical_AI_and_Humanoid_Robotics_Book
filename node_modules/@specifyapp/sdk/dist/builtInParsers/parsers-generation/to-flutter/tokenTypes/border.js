import { renderColor } from './color.js';
import { renderRadii } from './radii.js';
export function renderBorder(border, variableName) {
    const color = renderColor(border.color);
    if (!color)
        return [];
    const values = [
        {
            variableName,
            value: `BorderSide(width: ${Number.isInteger(border.width.value) ? border.width.value.toFixed(1) : border.width.value}, color: ${color})`,
            type: 'border',
        },
    ];
    if (!!border.rectangleCornerRadii) {
        values.push({
            variableName: `${variableName}BorderRadius`,
            value: renderRadii(border.rectangleCornerRadii),
            type: 'radii',
        });
    }
    return values;
}
//# sourceMappingURL=border.js.map