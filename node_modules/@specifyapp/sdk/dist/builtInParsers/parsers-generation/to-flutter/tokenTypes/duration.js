export function renderDuration(duration) {
    // Values are int, so we force number to be an int
    return `Duration(${duration.unit === 's' ? 'seconds:' : 'milliseconds:'} ${duration.value.toFixed(0)})`;
}
//# sourceMappingURL=duration.js.map