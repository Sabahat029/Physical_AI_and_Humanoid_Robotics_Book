import { renderShadow } from './shadow.js';
export function renderShadows(shadows, variableName) {
    return shadows.flatMap((v, i) => {
        const shadow = renderShadow(v);
        if (!shadow)
            return [];
        return [
            {
                value: shadow,
                variableName: `${variableName}${i}`,
                type: 'shadow',
            },
        ];
    });
}
//# sourceMappingURL=shadows.js.map