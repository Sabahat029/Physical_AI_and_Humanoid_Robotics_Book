import { colorToJson } from './color.js';
export function gradientToJson(value) {
    const colors = value.colorStops.flatMap(({ color, position }) => {
        const jsonColor = colorToJson(color);
        if (!jsonColor)
            return [];
        return [`${jsonColor} ${position * 100}%`];
    });
    switch (value.type) {
        case 'conic': {
            if (colors.length !== value.colorStops.length)
                return undefined;
            return `conic-gradient(from ${value.angle}deg at ${value.position}, ${colors.join(', ')})`;
        }
        case 'radial': {
            if (colors.length !== value.colorStops.length)
                return undefined;
            return `radial-gradient(circle at ${value.position}, ${colors.join(', ')})`;
        }
        case 'linear': {
            if (colors.length !== value.colorStops.length)
                return undefined;
            return `linear-gradient(${value.angle}deg, ${colors.join(', ')})`;
        }
    }
}
//# sourceMappingURL=gradient.js.map