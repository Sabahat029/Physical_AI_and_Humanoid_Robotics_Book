export const convertToCssClasses = (outputs) => {
    return outputs
        .map(output => {
        return `.${output.variableName} {
${Object.entries(output.value)
            .map(([key, value]) => `\t${key}: ${value};`)
            .join('\n')}
}`;
    })
        .join('\n');
};
//# sourceMappingURL=convertToCssClasses.js.map