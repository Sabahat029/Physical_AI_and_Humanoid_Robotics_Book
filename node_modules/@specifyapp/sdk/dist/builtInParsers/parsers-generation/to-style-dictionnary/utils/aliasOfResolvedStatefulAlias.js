import { makeFileName } from './makeFileName.js';
export function aliasOfResolvableAlias(alias) {
    const [fileName, tokenNesting] = makeFileName(alias.tokenState.type, alias.tokenState, alias.targetMode);
    return `{${fileName.replace(/\//g, '.')}.${!!tokenNesting ? tokenNesting + '.' : ''}${alias.tokenState.name}}`;
}
/* v8 ignore start */
export function aliasOfToken(token, mode) {
    const [fileName, tokenNesting] = makeFileName(token.type, token, mode);
    return `{${fileName.replace(/\//g, '.')}.${!!tokenNesting ? tokenNesting + '.' : ''}${token.name}}`;
}
/* v8 ignore stop */
//# sourceMappingURL=aliasOfResolvedStatefulAlias.js.map