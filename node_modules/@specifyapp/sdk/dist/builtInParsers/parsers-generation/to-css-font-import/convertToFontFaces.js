export const convertToFontFaces = (outputs) => {
    return outputs
        .map(output => {
        return `@font-face {
${Object.entries(output.value)
            .map(([key, value]) => `\t${key}: ${value};`)
            .join('\n')}
}`;
    })
        .join('\n');
};
//# sourceMappingURL=convertToFontFaces.js.map