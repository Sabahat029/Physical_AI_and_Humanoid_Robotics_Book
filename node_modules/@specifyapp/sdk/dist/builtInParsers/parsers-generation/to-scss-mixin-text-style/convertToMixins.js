export const convertToMixins = (outputs) => {
    return outputs
        .map(output => {
        return `@mixin ${output.variableName} {
${Object.entries(output.value)
            .map(([key, value]) => `\t${key}: ${value};`)
            .join('\n')}
}`;
    })
        .join('\n');
};
//# sourceMappingURL=convertToMixins.js.map