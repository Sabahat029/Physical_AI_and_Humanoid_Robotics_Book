import { renderColor } from './color.js';
import { renderDimension } from './dimension.js';
export function renderShadow(shadow) {
    return `${shadow.type === 'inner' ? 'inset ' : ''}${renderDimension(shadow.offsetX)} ${renderDimension(shadow.offsetY)} ${renderDimension(shadow.blurRadius)} ${renderDimension(shadow.spreadRadius)} ${renderColor(shadow.color) ?? ''}`;
}
//# sourceMappingURL=shadow.js.map