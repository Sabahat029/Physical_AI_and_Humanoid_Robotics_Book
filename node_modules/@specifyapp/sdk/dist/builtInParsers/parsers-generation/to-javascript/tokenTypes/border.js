import { renderColor } from './color.js';
export function renderBorder(border) {
    return `${border.width.value}${border.width.unit}${typeof border.style === 'string' ? ' ' + border.style : ''} ${renderColor(border.color)}`;
}
//# sourceMappingURL=border.js.map