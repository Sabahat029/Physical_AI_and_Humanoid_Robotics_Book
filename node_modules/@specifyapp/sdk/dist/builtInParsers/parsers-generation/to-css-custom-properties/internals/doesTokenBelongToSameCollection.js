export function doesTokenBelongToSameCollection(token1, token2) {
    const collection1 = token1.getCollection();
    const collection2 = token2.getCollection();
    if (!collection1 && !collection2)
        return true;
    if (!collection1 || !collection2)
        return false;
    return collection1.path.toString() === collection2.path.toString();
}
//# sourceMappingURL=doesTokenBelongToSameCollection.js.map