export function renderDuration(duration) {
    // React native only supports ms
    if (duration.unit === 'ms')
        return duration.value;
    else
        return duration.value * 1000;
}
//# sourceMappingURL=duration.js.map