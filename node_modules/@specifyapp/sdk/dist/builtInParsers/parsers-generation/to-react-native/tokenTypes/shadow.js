import { renderColor } from './color.js';
import { renderDimension } from './dimension.js';
export function renderShadow(shadow) {
    const variables = {
        type: shadow.type,
        offsetX: renderDimension(shadow.offsetX),
        offsetY: renderDimension(shadow.offsetY),
        color: renderColor(shadow.color),
    };
    if (!!shadow.blurRadius)
        variables.blurRadius = renderDimension(shadow.blurRadius);
    if (!!shadow.spreadRadius)
        variables.spreadRadius = renderDimension(shadow.spreadRadius);
    return variables;
}
//# sourceMappingURL=shadow.js.map