import { renderGradient } from './gradient.js';
export function renderGradients(gradients) {
    return gradients.map(gradient => {
        return renderGradient(gradient);
    });
}
//# sourceMappingURL=gradients.js.map