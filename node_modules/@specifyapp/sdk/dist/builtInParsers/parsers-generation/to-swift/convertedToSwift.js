import { upperFirst } from 'lodash-es';
function renderMap(input) {
    return Object.entries(input).reduce((acc, [name, value]) => {
        return acc + `    static let ${name} = ${value}\n`;
    }, '');
}
// When class name already exists, we prefix it
function sanitizeType(name) {
    switch (name) {
        case 'radius':
            return 'BorderRadius';
        case 'color':
        case 'font':
        case 'gradient':
        case 'duration':
            return `${name}_`;
        default:
            return name;
    }
}
// We scope all tokens in a class to avoid name conflicts as everything can be renamed
export function convertedToSwift(input, scopeName) {
    return `import SwiftUI

class ${scopeName} {
${Object.entries(input).reduce((acc, [type, map]) => {
        return (acc +
            `  class ${upperFirst(sanitizeType(type))} {\n` +
            renderMap(map) +
            '  }\n\n');
    }, '')}
}`;
}
//# sourceMappingURL=convertedToSwift.js.map