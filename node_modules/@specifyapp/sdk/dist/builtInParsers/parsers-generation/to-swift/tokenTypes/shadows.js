import { shadowToSwift } from './shadow.js';
export function shadowsToSwift(shadows, variableName) {
    return shadows.flatMap((v, i) => shadowToSwift(v, `${variableName}${i}`));
}
//# sourceMappingURL=shadows.js.map