import { numberToDouble } from '../utils.js';
export function spacingsToSwift(value, variableName) {
    return value.map((v, i) => ({
        variableName: `${variableName}${i}`,
        value: numberToDouble(v.value),
        type: 'spacing',
    }));
}
//# sourceMappingURL=spacings.js.map