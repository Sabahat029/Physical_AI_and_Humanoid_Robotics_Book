import { gradientToSwift } from './gradient.js';
export function gradientsToSwift(gradients, variableName) {
    return gradients.flatMap((v, i) => {
        const gradient = gradientToSwift(v);
        if (!gradient)
            return [];
        return [
            {
                value: gradient,
                variableName: `${variableName}${i}`,
                type: 'gradient',
            },
        ];
    });
}
//# sourceMappingURL=gradients.js.map