import { colorToSwift } from './color.js';
import { dimensionToSwift } from './dimension.js';
export function borderToSwift(border, variableName) {
    const color = colorToSwift(border.color);
    if (!color)
        return [];
    if (border.rectangleCornerRadii) {
        return [
            {
                variableName,
                value: `RoundedRectangle(cornerRadius: ${dimensionToSwift(border.rectangleCornerRadii[0])}).stroke(${color}, lineWidth: ${dimensionToSwift(border.width)})`,
                type: 'border',
            },
        ];
    }
    return [
        {
            variableName: `${variableName}Color`,
            value: color,
            type: 'color',
        },
        {
            variableName: `${variableName}Width`,
            value: dimensionToSwift(border.width),
            type: 'dimension',
        },
    ];
}
//# sourceMappingURL=border.js.map