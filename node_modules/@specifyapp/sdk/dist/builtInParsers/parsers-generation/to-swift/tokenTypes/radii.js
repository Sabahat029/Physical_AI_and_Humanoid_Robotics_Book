import { radiusToSwift } from './radius.js';
export function radiiToSwift(radii, variableName) {
    return radii.flatMap((v, i) => {
        return [
            {
                value: radiusToSwift(v),
                variableName: `${variableName}${i}`,
                type: 'radius',
            },
        ];
    });
}
//# sourceMappingURL=radii.js.map