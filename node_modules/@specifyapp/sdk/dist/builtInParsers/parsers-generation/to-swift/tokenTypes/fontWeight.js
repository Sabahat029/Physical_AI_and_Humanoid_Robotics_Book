function between(v, min, max) {
    return v >= min && v <= max;
}
export function fontWeightToSwift(fontWeight) {
    if (typeof fontWeight === 'string') {
        switch (fontWeight) {
            case 'thin':
            case 'hairline':
                return 'Font.Weight.thin';
            case 'ultra-light':
            case 'extra-light':
                return 'Font.Weight.ultraLight';
            case 'light':
            case 'book':
                return 'Font.Weight.light';
            case 'normal':
            case 'regular':
                return 'Font.Weight.regular';
            case 'medium':
                return 'Font.Weight.medium';
            case 'semi-bold':
            case 'demi-bold':
                return 'Font.Weight.semibold';
            case 'bold':
                return 'Font.Weight.bold';
            case 'ultra-bold':
            case 'extra-bold':
            case 'heavy':
                return 'Font.Weight.heavy';
            case 'black':
            case 'extra-black':
            case 'ultra-black':
                return 'Font.Weight.black';
        }
    }
    else {
        if (between(fontWeight, 0, 150)) {
            return 'Font.Weight.thin';
        }
        else if (between(fontWeight, 151, 250)) {
            return 'Font.Weight.ultraLight';
        }
        else if (between(fontWeight, 251, 350)) {
            return 'Font.Weight.light';
        }
        else if (between(fontWeight, 351, 450)) {
            return 'Font.Weight.regular';
        }
        else if (between(fontWeight, 451, 550)) {
            return 'Font.Weight.medium';
        }
        else if (between(fontWeight, 551, 650)) {
            return 'Font.Weight.semibold';
        }
        else if (between(fontWeight, 651, 750)) {
            return 'Font.Weight.bold';
        }
        else if (between(fontWeight, 751, 850)) {
            return 'Font.Weight.heavy';
        }
        else if (between(fontWeight, 851, 1000)) {
            return 'Font.Weight.black';
        }
    }
}
//# sourceMappingURL=fontWeight.js.map