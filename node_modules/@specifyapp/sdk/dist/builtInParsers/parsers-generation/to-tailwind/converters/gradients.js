import { gradientToTailwind } from './gradient.js';
export function gradientsToTailwind(value) {
    return `${value
        .flatMap(v => {
        const converted = gradientToTailwind(v);
        return !!converted ? [converted] : [];
    })
        .join(', ')}`;
}
//# sourceMappingURL=gradients.js.map