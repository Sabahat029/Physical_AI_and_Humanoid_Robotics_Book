export function durationToKotlin(duration) {
    // Durations are in milliseconds in Kotlin
    return (duration.unit === 's' ? duration.value * 1000 : duration.value).toString();
}
//# sourceMappingURL=duration.js.map