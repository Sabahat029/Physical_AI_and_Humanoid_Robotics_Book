export function makeNumberRounder({ mode, precision, }) {
    const roundFn = mode === 'auto' ? Math.round : mode == 'up' ? Math.ceil : Math.floor;
    const factor = Math.pow(10, precision);
    return (number) => roundFn(number * factor) / factor;
}
//# sourceMappingURL=makeNumberRounder.js.map