export function regexOfOption(option) {
    if (option === undefined) {
        return new RegExp('');
    }
    return new RegExp(typeof option.regex === 'string' ? option.regex : option.regex.pattern, typeof option.regex === 'string' ? undefined : option.regex.flags);
}
//# sourceMappingURL=regexOfOption.js.map