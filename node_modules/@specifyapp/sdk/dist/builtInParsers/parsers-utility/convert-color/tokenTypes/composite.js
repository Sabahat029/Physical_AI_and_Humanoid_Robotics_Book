import { colorToTarget } from '../colorToTarget.js';
export function getCompositeUpdateValue(value, target) {
    const { color } = value;
    if (!color)
        return;
    return { color: colorToTarget(color, target) };
}
//# sourceMappingURL=composite.js.map