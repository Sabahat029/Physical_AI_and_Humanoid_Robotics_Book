/* v8 ignore start */
export function matchToken(tokenMatcher, missingFn, token) {
    switch (token.type) {
        case 'arcDegreeNumber':
            // @ts-ignore - Type too complex
            return (tokenMatcher[token.type] ?? missingFn)(token);
        case 'array':
            return (tokenMatcher[token.type] ?? missingFn)(token);
        case 'bitmap':
            return (tokenMatcher[token.type] ?? missingFn)(token);
        case 'bitmapFormat':
            return (tokenMatcher[token.type] ?? missingFn)(token);
        case 'blur':
            return (tokenMatcher[token.type] ?? missingFn)(token);
        case 'boolean':
            return (tokenMatcher[token.type] ?? missingFn)(token);
        case 'border':
            return (tokenMatcher[token.type] ?? missingFn)(token);
        case 'borderStyle':
            return (tokenMatcher[token.type] ?? missingFn)(token);
        case 'borderStyleLineCap':
            return (tokenMatcher[token.type] ?? missingFn)(token);
        case 'breakpoint':
            return (tokenMatcher[token.type] ?? missingFn)(token);
        case 'color':
            return (tokenMatcher[token.type] ?? missingFn)(token);
        case 'cubicBezier':
            return (tokenMatcher[token.type] ?? missingFn)(token);
        case 'dimension':
            return (tokenMatcher[token.type] ?? missingFn)(token);
        case 'dimensionUnit':
            return (tokenMatcher[token.type] ?? missingFn)(token);
        case 'duration':
            return (tokenMatcher[token.type] ?? missingFn)(token);
        case 'durationUnit':
            return (tokenMatcher[token.type] ?? missingFn)(token);
        case 'font':
            return (tokenMatcher[token.type] ?? missingFn)(token);
        case 'fontFamily':
            return (tokenMatcher[token.type] ?? missingFn)(token);
        case 'fontFeature':
            return (tokenMatcher[token.type] ?? missingFn)(token);
        case 'fontFeatures':
            return (tokenMatcher[token.type] ?? missingFn)(token);
        case 'fontFormat':
            return (tokenMatcher[token.type] ?? missingFn)(token);
        case 'fontStyle':
            return (tokenMatcher[token.type] ?? missingFn)(token);
        case 'fontWeight':
            return (tokenMatcher[token.type] ?? missingFn)(token);
        case 'gradient':
            return (tokenMatcher[token.type] ?? missingFn)(token);
        case 'gradients':
            return (tokenMatcher[token.type] ?? missingFn)(token);
        case 'hexadecimalColorString':
            return (tokenMatcher[token.type] ?? missingFn)(token);
        case 'integerNumber':
            return (tokenMatcher[token.type] ?? missingFn)(token);
        case 'null':
            return (tokenMatcher[token.type] ?? missingFn)(token);
        case 'number':
            return (tokenMatcher[token.type] ?? missingFn)(token);
        case 'object':
            return (tokenMatcher[token.type] ?? missingFn)(token);
        case 'opacity':
            return (tokenMatcher[token.type] ?? missingFn)(token);
        case 'percentageNumber':
            return (tokenMatcher[token.type] ?? missingFn)(token);
        case 'positiveIntegerNumber':
            return (tokenMatcher[token.type] ?? missingFn)(token);
        case 'positiveNumber':
            return (tokenMatcher[token.type] ?? missingFn)(token);
        case 'radii':
            return (tokenMatcher[token.type] ?? missingFn)(token);
        case 'radius':
            return (tokenMatcher[token.type] ?? missingFn)(token);
        case 'rgbColorNumber':
            return (tokenMatcher[token.type] ?? missingFn)(token);
        case 'shadow':
            return (tokenMatcher[token.type] ?? missingFn)(token);
        case 'shadowType':
            return (tokenMatcher[token.type] ?? missingFn)(token);
        case 'shadows':
            return (tokenMatcher[token.type] ?? missingFn)(token);
        case 'spacing':
            return (tokenMatcher[token.type] ?? missingFn)(token);
        case 'spacings':
            return (tokenMatcher[token.type] ?? missingFn)(token);
        case 'stepsTimingFunction':
            return (tokenMatcher[token.type] ?? missingFn)(token);
        case 'string':
            return (tokenMatcher[token.type] ?? missingFn)(token);
        case 'textAlignHorizontal':
            return (tokenMatcher[token.type] ?? missingFn)(token);
        case 'textAlignVertical':
            return (tokenMatcher[token.type] ?? missingFn)(token);
        case 'textDecoration':
            return (tokenMatcher[token.type] ?? missingFn)(token);
        case 'textStyle':
            return (tokenMatcher[token.type] ?? missingFn)(token);
        case 'textTransform':
            return (tokenMatcher[token.type] ?? missingFn)(token);
        case 'transition':
            return (tokenMatcher[token.type] ?? missingFn)(token);
        case 'vector':
            return (tokenMatcher[token.type] ?? missingFn)(token);
        case 'vectorFormat':
            return (tokenMatcher[token.type] ?? missingFn)(token);
        case 'zIndex':
            return (tokenMatcher[token.type] ?? missingFn)(token);
        case 'zeroToOneNumber':
            return (tokenMatcher[token.type] ?? missingFn)(token);
    }
}
/* v8 ignore stop */
//# sourceMappingURL=tokenMatcher.js.map