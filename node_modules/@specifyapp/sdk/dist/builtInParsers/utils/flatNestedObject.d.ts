import { TokenState } from '@specifyapp/specify-design-token-format';
type nestedValueWithToken<T> = {
    [tokenName: string]: T;
};
/**
 * `path` is expected to be a object key path separated by `.`,
 * rather than working with nested object, everything will be nested at the end.
 * Useful and when working with groups
 * Example:
 * - `hello` -> `{ hello: { ... } }`
 * - `hello.world` -> `{ hello: { world: { ... } } }`
 */
type flatNestedObject<T> = {
    [path: string]: nestedValueWithToken<T> | T;
};
export declare function toNestedObject<T>(flatObject: flatNestedObject<T>): {};
export declare function makeDefaultPath(token: TokenState): string;
type convertTokenReturn<T> = {
    value: T;
    path?: string;
    name?: string;
};
export declare function tokensToFlatObject<T>(tokens: Array<TokenState>, convertToken: (token: TokenState) => convertTokenReturn<T> | Array<convertTokenReturn<T>> | undefined): flatNestedObject<T>;
export {};
//# sourceMappingURL=flatNestedObject.d.ts.map