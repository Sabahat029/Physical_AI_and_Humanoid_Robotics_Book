/* v8 ignore start */
export function matchJsonValue(tokenMatcher, missingFn, token, targetMode) {
    switch (token.type) {
        case 'arcDegreeNumber':
            return (tokenMatcher[token.type] ?? missingFn)(token.getJSONValue({
                resolveAliases: true,
                allowUnresolvable: false,
                targetMode,
            }));
        case 'array':
            return (tokenMatcher[token.type] ?? missingFn)(token.getJSONValue({
                resolveAliases: true,
                allowUnresolvable: false,
                targetMode,
            }));
        case 'bitmap':
            return (tokenMatcher[token.type] ?? missingFn)(token.getJSONValue({
                resolveAliases: true,
                allowUnresolvable: false,
                targetMode,
            }));
        case 'bitmapFormat':
            return (tokenMatcher[token.type] ?? missingFn)(token.getJSONValue({
                resolveAliases: true,
                allowUnresolvable: false,
                targetMode,
            }));
        case 'blur':
            return (tokenMatcher[token.type] ?? missingFn)(token.getJSONValue({
                resolveAliases: true,
                allowUnresolvable: false,
                targetMode,
            }));
        case 'boolean':
            return (tokenMatcher[token.type] ?? missingFn)(token.getJSONValue({
                resolveAliases: true,
                allowUnresolvable: false,
                targetMode,
            }));
        case 'border':
            return (tokenMatcher[token.type] ?? missingFn)(token.getJSONValue({
                resolveAliases: true,
                allowUnresolvable: false,
                targetMode,
            }));
        case 'borderStyle':
            return (tokenMatcher[token.type] ?? missingFn)(token.getJSONValue({
                resolveAliases: true,
                allowUnresolvable: false,
                targetMode,
            }));
        case 'borderStyleLineCap':
            return (tokenMatcher[token.type] ?? missingFn)(token.getJSONValue({
                resolveAliases: true,
                allowUnresolvable: false,
                targetMode,
            }));
        case 'breakpoint':
            return (tokenMatcher[token.type] ?? missingFn)(token.getJSONValue({
                resolveAliases: true,
                allowUnresolvable: false,
                targetMode,
            }));
        case 'color':
            return (tokenMatcher[token.type] ?? missingFn)(token.getJSONValue({
                resolveAliases: true,
                allowUnresolvable: false,
                targetMode,
            }));
        case 'cubicBezier':
            return (tokenMatcher[token.type] ?? missingFn)(token.getJSONValue({
                resolveAliases: true,
                allowUnresolvable: false,
                targetMode,
            }));
        case 'dimension':
            return (tokenMatcher[token.type] ?? missingFn)(token.getJSONValue({
                resolveAliases: true,
                allowUnresolvable: false,
                targetMode,
            }));
        case 'dimensionUnit':
            return (tokenMatcher[token.type] ?? missingFn)(token.getJSONValue({
                resolveAliases: true,
                allowUnresolvable: false,
                targetMode,
            }));
        case 'duration':
            return (tokenMatcher[token.type] ?? missingFn)(token.getJSONValue({
                resolveAliases: true,
                allowUnresolvable: false,
                targetMode,
            }));
        case 'durationUnit':
            return (tokenMatcher[token.type] ?? missingFn)(token.getJSONValue({
                resolveAliases: true,
                allowUnresolvable: false,
                targetMode,
            }));
        case 'font':
            return (tokenMatcher[token.type] ?? missingFn)(token.getJSONValue({
                resolveAliases: true,
                allowUnresolvable: false,
                targetMode,
            }));
        case 'fontFamily':
            return (tokenMatcher[token.type] ?? missingFn)(token.getJSONValue({
                resolveAliases: true,
                allowUnresolvable: false,
                targetMode,
            }));
        case 'fontFeature':
            return (tokenMatcher[token.type] ?? missingFn)(token.getJSONValue({
                resolveAliases: true,
                allowUnresolvable: false,
                targetMode,
            }));
        case 'fontFeatures':
            return (tokenMatcher[token.type] ?? missingFn)(token.getJSONValue({
                resolveAliases: true,
                allowUnresolvable: false,
                targetMode,
            }));
        case 'fontFormat':
            return (tokenMatcher[token.type] ?? missingFn)(token.getJSONValue({
                resolveAliases: true,
                allowUnresolvable: false,
                targetMode,
            }));
        case 'fontStyle':
            return (tokenMatcher[token.type] ?? missingFn)(token.getJSONValue({
                resolveAliases: true,
                allowUnresolvable: false,
                targetMode,
            }));
        case 'fontWeight':
            return (tokenMatcher[token.type] ?? missingFn)(token.getJSONValue({
                resolveAliases: true,
                allowUnresolvable: false,
                targetMode,
            }));
        case 'gradient':
            return (tokenMatcher[token.type] ?? missingFn)(token.getJSONValue({
                resolveAliases: true,
                allowUnresolvable: false,
                targetMode,
            }));
        case 'gradients':
            return (tokenMatcher[token.type] ?? missingFn)(token.getJSONValue({
                resolveAliases: true,
                allowUnresolvable: false,
                targetMode,
            }));
        case 'hexadecimalColorString':
            return (tokenMatcher[token.type] ?? missingFn)(token.getJSONValue({
                resolveAliases: true,
                allowUnresolvable: false,
                targetMode,
            }));
        case 'integerNumber':
            return (tokenMatcher[token.type] ?? missingFn)(token.getJSONValue({
                resolveAliases: true,
                allowUnresolvable: false,
                targetMode,
            }));
        case 'null':
            return (tokenMatcher[token.type] ?? missingFn)(token.getJSONValue({
                resolveAliases: true,
                allowUnresolvable: false,
                targetMode,
            }));
        case 'number':
            return (tokenMatcher[token.type] ?? missingFn)(token.getJSONValue({
                resolveAliases: true,
                allowUnresolvable: false,
                targetMode,
            }));
        case 'object':
            return (tokenMatcher[token.type] ?? missingFn)(token.getJSONValue({
                resolveAliases: true,
                allowUnresolvable: false,
                targetMode,
            }));
        case 'opacity':
            return (tokenMatcher[token.type] ?? missingFn)(token.getJSONValue({
                resolveAliases: true,
                allowUnresolvable: false,
                targetMode,
            }));
        case 'percentageNumber':
            return (tokenMatcher[token.type] ?? missingFn)(token.getJSONValue({
                resolveAliases: true,
                allowUnresolvable: false,
                targetMode,
            }));
        case 'positiveIntegerNumber':
            return (tokenMatcher[token.type] ?? missingFn)(token.getJSONValue({
                resolveAliases: true,
                allowUnresolvable: false,
                targetMode,
            }));
        case 'positiveNumber':
            return (tokenMatcher[token.type] ?? missingFn)(token.getJSONValue({
                resolveAliases: true,
                allowUnresolvable: false,
                targetMode,
            }));
        case 'radii':
            return (tokenMatcher[token.type] ?? missingFn)(token.getJSONValue({
                resolveAliases: true,
                allowUnresolvable: false,
                targetMode,
            }));
        case 'radius':
            return (tokenMatcher[token.type] ?? missingFn)(token.getJSONValue({
                resolveAliases: true,
                allowUnresolvable: false,
                targetMode,
            }));
        case 'rgbColorNumber':
            return (tokenMatcher[token.type] ?? missingFn)(token.getJSONValue({
                resolveAliases: true,
                allowUnresolvable: false,
                targetMode,
            }));
        case 'shadow':
            return (tokenMatcher[token.type] ?? missingFn)(token.getJSONValue({
                resolveAliases: true,
                allowUnresolvable: false,
                targetMode,
            }));
        case 'shadowType':
            return (tokenMatcher[token.type] ?? missingFn)(token.getJSONValue({
                resolveAliases: true,
                allowUnresolvable: false,
                targetMode,
            }));
        case 'shadows':
            return (tokenMatcher[token.type] ?? missingFn)(token.getJSONValue({
                resolveAliases: true,
                allowUnresolvable: false,
                targetMode,
            }));
        case 'spacing':
            return (tokenMatcher[token.type] ?? missingFn)(token.getJSONValue({
                resolveAliases: true,
                allowUnresolvable: false,
                targetMode,
            }));
        case 'spacings':
            return (tokenMatcher[token.type] ?? missingFn)(token.getJSONValue({
                resolveAliases: true,
                allowUnresolvable: false,
                targetMode,
            }));
        case 'stepsTimingFunction':
            return (tokenMatcher[token.type] ?? missingFn)(token.getJSONValue({
                resolveAliases: true,
                allowUnresolvable: false,
                targetMode,
            }));
        case 'string':
            return (tokenMatcher[token.type] ?? missingFn)(token.getJSONValue({
                resolveAliases: true,
                allowUnresolvable: false,
                targetMode,
            }));
        case 'textAlignHorizontal':
            return (tokenMatcher[token.type] ?? missingFn)(token.getJSONValue({
                resolveAliases: true,
                allowUnresolvable: false,
                targetMode,
            }));
        case 'textAlignVertical':
            return (tokenMatcher[token.type] ?? missingFn)(token.getJSONValue({
                resolveAliases: true,
                allowUnresolvable: false,
                targetMode,
            }));
        case 'textDecoration':
            return (tokenMatcher[token.type] ?? missingFn)(token.getJSONValue({
                resolveAliases: true,
                allowUnresolvable: false,
                targetMode,
            }));
        case 'textStyle':
            return (tokenMatcher[token.type] ?? missingFn)(token.getJSONValue({
                resolveAliases: true,
                allowUnresolvable: false,
                targetMode,
            }));
        case 'textTransform':
            return (tokenMatcher[token.type] ?? missingFn)(token.getJSONValue({
                resolveAliases: true,
                allowUnresolvable: false,
                targetMode,
            }));
        case 'transition':
            return (tokenMatcher[token.type] ?? missingFn)(token.getJSONValue({
                resolveAliases: true,
                allowUnresolvable: false,
                targetMode,
            }));
        case 'vector':
            return (tokenMatcher[token.type] ?? missingFn)(token.getJSONValue({
                resolveAliases: true,
                allowUnresolvable: false,
                targetMode,
            }));
        case 'vectorFormat':
            return (tokenMatcher[token.type] ?? missingFn)(token.getJSONValue({
                resolveAliases: true,
                allowUnresolvable: false,
                targetMode,
            }));
        case 'zIndex':
            return (tokenMatcher[token.type] ?? missingFn)(token.getJSONValue({
                resolveAliases: true,
                allowUnresolvable: false,
                targetMode,
            }));
        case 'zeroToOneNumber':
            return (tokenMatcher[token.type] ?? missingFn)(token.getJSONValue({
                resolveAliases: true,
                allowUnresolvable: false,
                targetMode,
            }));
    }
}
/* v8 ignore stop */
//# sourceMappingURL=jsonValueMatcher.js.map