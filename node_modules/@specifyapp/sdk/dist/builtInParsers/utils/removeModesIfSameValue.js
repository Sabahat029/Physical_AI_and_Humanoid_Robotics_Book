export function removeModesIfSameValue(valueByMode) {
    const values = Object.values(valueByMode);
    if (values.length === 0)
        return valueByMode;
    if (values.every(v => v === values[0])) {
        return values[0];
    }
    else {
        return valueByMode;
    }
}
//# sourceMappingURL=removeModesIfSameValue.js.map