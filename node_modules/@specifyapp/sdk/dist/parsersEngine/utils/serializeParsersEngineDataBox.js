/**
 * Serialize a data box to a serialized data box
 * @param dataBox
 */
export function serializeParsersEngineDataBox(dataBox) {
    if (dataBox.type === 'SDTF Engine') {
        const { tokenTree, metadata } = dataBox.engine.exportEngineState();
        return {
            type: 'SDTF',
            graph: tokenTree,
            metadata,
        };
    }
    return dataBox;
}
//# sourceMappingURL=serializeParsersEngineDataBox.js.map