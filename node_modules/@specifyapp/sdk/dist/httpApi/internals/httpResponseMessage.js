export function makeHttpResponseInformationMessage(content, location) {
    const message = {
        type: 'information',
        content,
    };
    if (location) {
        message.location = location;
    }
    return message;
}
export function makeHttpResponseWarningMessage(errorKey, content, location) {
    const message = {
        type: 'warning',
        content,
        errorKey,
    };
    if (location) {
        message.location = location;
    }
    return message;
}
export function makeHttpResponseErrorMessage(errorKey, content, location) {
    const message = {
        type: 'error',
        content,
        errorKey,
    };
    if (location) {
        message.location = location;
    }
    return message;
}
//# sourceMappingURL=httpResponseMessage.js.map