import { promises as fs } from 'node:fs';
import { generateEmptyFile } from './fileSystem.js';
export async function replaceFile(relativeFilePath, content) {
    const absoluteFilePath = await generateEmptyFile(relativeFilePath);
    const finalContent = typeof content === 'string' || Buffer.isBuffer(content)
        ? content
        : JSON.stringify(content, null, 2);
    await fs.writeFile(absoluteFilePath, finalContent, {
        encoding: 'utf8',
    });
}
//# sourceMappingURL=replaceFile.js.map