import { DEFAULT_UNRESOLVABLE_STRATEGY } from '../aliasStrategies/throwOnUnresolvable.js';
/**
 * Converts a `TokenState<'zIndex'>` to a css value.
 * E.g: `10`
 */
export function zIndexToCss(aliasStrategy, unresolvableAliasStrategy = DEFAULT_UNRESOLVABLE_STRATEGY) {
    return (tokenState) => tokenState
        .getStatefulValueResult()
        .mapResolvableTopLevelAlias(aliasStrategy)
        .mapUnresolvableTopLevelAlias(alias => unresolvableAliasStrategy(tokenState, alias))
        .mapTopLevelValue(value => value
        .mapRawValue(rawValue => rawZIndexToCss(rawValue))
        .mapResolvableModeLevelAlias(aliasStrategy)
        .mapUnresolvableModeLevelAlias(alias => unresolvableAliasStrategy(tokenState, alias))
        .unwrap())
        .unwrap();
}
/**
 * Converts a `RawValueSignature<'zIndex'>` to css.
 */
export function rawZIndexToCss(zIndex) {
    return zIndex.toString();
}
/**
 * Converts a zIndex to css.
 */
export function zIndexValueToCss(zIndex) {
    return zIndex.toString();
}
//# sourceMappingURL=zIndex.js.map