export function unwrapInnerValue(innerValue, aliasStrategy, unresolvableAliasStrategy) {
    return innerValue
        .mapResolvableValueLevelAlias(aliasStrategy)
        .mapUnresolvableValueLevelAlias(alias => unresolvableAliasStrategy(innerValue.tokenState, alias))
        .unwrap();
}
//# sourceMappingURL=unwrapInnerValue.js.map