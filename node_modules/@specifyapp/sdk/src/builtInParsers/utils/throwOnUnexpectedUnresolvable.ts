import { SpecifyError, specifyErrors } from '../../errors/index.js';

export function throwOnUnexpectedUnresolvable(): never {
  throw new SpecifyError({
    errorKey: specifyErrors.PARSERS_ENGINE_UNKNOWN_ERROR.errorKey,
    publicMessage: `Only fully resolvable tokens are supposed to be processed`,
  });
}
