export function removeModesIfSameValue<Value>(valueByMode: { [mode: string]: Value }) {
  const values = Object.values(valueByMode);

  if (values.length === 0) return valueByMode;

  if (values.every(v => v === values[0])) {
    return values[0];
  } else {
    return valueByMode;
  }
}
