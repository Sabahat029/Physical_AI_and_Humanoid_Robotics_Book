import { TokenState } from '@specifyapp/specify-design-token-format';

export const filterAuthorizedToken =
  (authorizedTypes: Array<TokenState['type']>) => (token: TokenState) =>
    authorizedTypes.includes(token.type) ? token : undefined;
