import { SpecifyDurationValue } from '@specifyapp/specify-design-token-format';
import { NumberRounder } from '../makeNumberRounder.js';

export function getDurationUpdateValue(duration: SpecifyDurationValue, roundNumber: NumberRounder) {
  duration.value = roundNumber(duration.value);
  return duration;
}
