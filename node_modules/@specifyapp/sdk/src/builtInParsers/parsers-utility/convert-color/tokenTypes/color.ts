import { SpecifyColorValue } from '@specifyapp/specify-design-token-format';
import { colorToTarget } from '../colorToTarget.js';

export function getColorUpdateValue(color: SpecifyColorValue, target: SpecifyColorValue['model']) {
  return colorToTarget(color, target);
}
