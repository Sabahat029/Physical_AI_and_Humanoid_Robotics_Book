import { SpecifyDurationValue } from '@specifyapp/specify-design-token-format';

export function durationToKotlin(duration: SpecifyDurationValue) {
  // Durations are in milliseconds in Kotlin
  return (duration.unit === 's' ? duration.value * 1000 : duration.value).toString();
}
