import { SpecifySpacingsValue } from '@specifyapp/specify-design-token-format';
import { spacingToKotlin } from './spacing.js';

export function spacingsToKotlin(value: SpecifySpacingsValue) {
  return `arrayOf(${value.map(spacingToKotlin).join(', ')})`;
}
