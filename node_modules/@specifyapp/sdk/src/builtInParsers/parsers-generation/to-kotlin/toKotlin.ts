import { DeriveMakeParserFunctionFromDefinition } from '../../internals/createBuiltInParserDefinition.js';
import { wrapBuiltInParserHandler } from '../../internals/wrapBuiltInParserHandler.js';
import { toKotlinParserDefinition, ToKotlinParserDefinition } from './definition.js';
import { toKotlinHandler } from './toKotlinHandler.js';

/* v8 ignore start */
export const toKotlin: DeriveMakeParserFunctionFromDefinition<ToKotlinParserDefinition> =
  ({ options, output }) =>
  (dataBox, toolbox) =>
    wrapBuiltInParserHandler(toKotlinParserDefinition, toKotlinHandler, {
      dataBox,
      toolbox,
      options,
      output,
    });
/* v8 ignore stop */
