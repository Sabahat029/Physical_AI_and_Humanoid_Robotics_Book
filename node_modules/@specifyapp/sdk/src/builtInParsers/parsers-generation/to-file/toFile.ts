import { DeriveMakeParserFunctionFromDefinition } from '../../internals/createBuiltInParserDefinition.js';
import { wrapBuiltInParserHandler } from '../../internals/wrapBuiltInParserHandler.js';
import { toFileParserDefinition, ToFileParserDefinition } from './definition.js';
import { toFileHandler } from './toFileHandler.js';

/* v8 ignore start */
export const toFile: DeriveMakeParserFunctionFromDefinition<ToFileParserDefinition> =
  ({ options, output }) =>
  (dataBox, toolbox) =>
    wrapBuiltInParserHandler(toFileParserDefinition, toFileHandler, {
      dataBox,
      toolbox,
      options,
      output,
    });

/* v8 ignore stop */
