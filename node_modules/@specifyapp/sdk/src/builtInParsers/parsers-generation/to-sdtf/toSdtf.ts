import { DeriveMakeParserFunctionFromDefinition } from '../../internals/createBuiltInParserDefinition.js';
import { wrapBuiltInParserHandler } from '../../internals/wrapBuiltInParserHandler.js';
import { toSdtfParserDefinition, ToSdtfParserDefinition } from './definition.js';
import { toSdtfHandler } from './toSdtfHandler.js';

/* v8 ignore start */
export const toSdtf: DeriveMakeParserFunctionFromDefinition<ToSdtfParserDefinition> =
  ({ options, output }) =>
  (dataBox, toolbox) =>
    wrapBuiltInParserHandler(toSdtfParserDefinition, toSdtfHandler, {
      dataBox,
      toolbox,
      options,
      output,
    });
/* v8 ignore stop */
