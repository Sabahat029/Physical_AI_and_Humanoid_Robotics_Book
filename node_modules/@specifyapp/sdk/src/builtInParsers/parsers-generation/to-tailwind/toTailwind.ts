import { DeriveMakeParserFunctionFromDefinition } from '../../internals/createBuiltInParserDefinition.js';
import { wrapBuiltInParserHandler } from '../../internals/wrapBuiltInParserHandler.js';
import { toTailwindParserDefinition, ToTailwindParserDefinition } from './definition.js';
import { toTailwindHandler } from './toTailwindHandler.js';

/* v8 ignore start */
export const toTailwind: DeriveMakeParserFunctionFromDefinition<ToTailwindParserDefinition> =
  ({ options, output }) =>
  (dataBox, toolbox) =>
    wrapBuiltInParserHandler(toTailwindParserDefinition, toTailwindHandler, {
      dataBox,
      toolbox,
      options,
      output,
    });
/* v8 ignore stop */
