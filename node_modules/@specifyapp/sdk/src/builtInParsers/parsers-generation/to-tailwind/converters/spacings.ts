import { SpecifySpacingsValue } from '@specifyapp/specify-design-token-format';
import { dimensionToTailwind } from './dimension.js';

export function spacingsToTailwind(value: SpecifySpacingsValue) {
  return `${value.map(dimensionToTailwind).join(' ')}`;
}
