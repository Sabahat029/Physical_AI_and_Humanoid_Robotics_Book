import { DeriveMakeParserFunctionFromDefinition } from '../../internals/createBuiltInParserDefinition.js';
import { wrapBuiltInParserHandler } from '../../internals/wrapBuiltInParserHandler.js';
import {
  toStyleDictionaryParserDefinition,
  ToStyleDictionaryParserDefinition,
} from './definition.js';
import { toStyleDictionaryHandler } from './toStyleDictionaryHandler.js';

/* v8 ignore start */
export const toStyleDictionary: DeriveMakeParserFunctionFromDefinition<
  ToStyleDictionaryParserDefinition
> =
  ({ options, output }) =>
  (dataBox, toolbox) =>
    wrapBuiltInParserHandler(toStyleDictionaryParserDefinition, toStyleDictionaryHandler, {
      dataBox,
      toolbox,
      options,
      output,
    });
/* v8 ignore stop */
