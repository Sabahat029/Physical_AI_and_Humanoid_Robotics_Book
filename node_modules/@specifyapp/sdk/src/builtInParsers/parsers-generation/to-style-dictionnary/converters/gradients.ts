import { RawValueSignature } from '@specifyapp/specify-design-token-format';
import { gradientToStyleDictionary } from './gradient.js';

export const gradientsToStyleDictionary = (gradients: RawValueSignature<'gradients'>) =>
  gradients.map(gradient => gradient.mapPrimitiveValue(gradientToStyleDictionary).unwrapValue());
