import { RawValueSignature } from '@specifyapp/specify-design-token-format';
import { shadowToStyleDictionary } from './shadow.js';

export const shadowsToStyleDictionary = (shadows: RawValueSignature<'shadows'>) =>
  shadows.map(shadow => shadow.mapPrimitiveValue(shadowToStyleDictionary).unwrapValue());
