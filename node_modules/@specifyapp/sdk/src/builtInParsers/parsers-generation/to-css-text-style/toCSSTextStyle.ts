import { DeriveMakeParserFunctionFromDefinition } from '../../internals/createBuiltInParserDefinition.js';
import { wrapBuiltInParserHandler } from '../../internals/wrapBuiltInParserHandler.js';
import { ToCssTextStyleParserDefinition, toCssTextStyleParserDefinition } from './definition.js';
import { toCssTextStyleHandler } from './toCssTextStyleHandler.js';

/* v8 ignore start */
export const toCSSTextStyle: DeriveMakeParserFunctionFromDefinition<
  ToCssTextStyleParserDefinition
> =
  ({ options, output }) =>
  (dataBox, toolbox) =>
    wrapBuiltInParserHandler(toCssTextStyleParserDefinition, toCssTextStyleHandler, {
      dataBox,
      toolbox,
      options,
      output,
    });
/* v8 ignore stop */
