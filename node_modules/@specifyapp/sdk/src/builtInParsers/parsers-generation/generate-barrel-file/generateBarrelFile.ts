import { DeriveMakeParserFunctionFromDefinition } from '../../internals/createBuiltInParserDefinition.js';
import { wrapBuiltInParserHandler } from '../../internals/wrapBuiltInParserHandler.js';
import {
  generateBarrelFileParserDefinition,
  GenerateBarrelFileParserDefinition,
} from './definition.js';
import { generateBarrelFileHandler } from './generateBarrelFileHandler.js';

export const generateBarrelFile: DeriveMakeParserFunctionFromDefinition<
  GenerateBarrelFileParserDefinition
> =
  ({ options, output }) =>
  (dataBox, toolbox) =>
    wrapBuiltInParserHandler(generateBarrelFileParserDefinition, generateBarrelFileHandler, {
      dataBox,
      toolbox,
      options,
      output,
    });
