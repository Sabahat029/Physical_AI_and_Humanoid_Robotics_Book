import { SpecifyShadowsValue } from '@specifyapp/specify-design-token-format';
import { renderShadow } from './shadow.js';

export function renderShadows(shadows: SpecifyShadowsValue) {
  return shadows.map(renderShadow);
}
