import { SpecifySpacingsValue } from '@specifyapp/specify-design-token-format';
import { renderSpacing } from './spacing.js';

export function renderSpacings(spacings: SpecifySpacingsValue) {
  return [{ name: '', value: spacings.map(renderSpacing) }];
}
