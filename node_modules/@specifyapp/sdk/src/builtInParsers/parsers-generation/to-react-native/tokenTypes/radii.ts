import { SpecifyRadiiValue } from '@specifyapp/specify-design-token-format';
import { renderRadius } from './radius.js';

export function renderRadii(radii: SpecifyRadiiValue) {
  return radii.map(renderRadius);
}
