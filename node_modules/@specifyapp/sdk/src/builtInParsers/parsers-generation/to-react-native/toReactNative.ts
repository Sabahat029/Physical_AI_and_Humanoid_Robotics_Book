import { DeriveMakeParserFunctionFromDefinition } from '../../internals/createBuiltInParserDefinition.js';
import { wrapBuiltInParserHandler } from '../../internals/wrapBuiltInParserHandler.js';
import { toReactNativeParserDefinition, ToReactNativeParserDefinition } from './definition.js';
import { toReactNativeHandler } from './toReactNativeHandler.js';

/* v8 ignore start */
export const toReactNative: DeriveMakeParserFunctionFromDefinition<ToReactNativeParserDefinition> =
  ({ options, output }) =>
  (dataBox, toolbox) =>
    wrapBuiltInParserHandler(toReactNativeParserDefinition, toReactNativeHandler, {
      dataBox,
      toolbox,
      options,
      output,
    });
/* v8 ignore stop */
