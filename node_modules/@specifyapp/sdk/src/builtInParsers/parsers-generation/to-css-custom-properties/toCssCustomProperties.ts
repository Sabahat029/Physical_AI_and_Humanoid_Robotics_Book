import type { DeriveMakeParserFunctionFromDefinition } from '../../internals/createBuiltInParserDefinition.js';
import { wrapBuiltInParserHandler } from '../../internals/wrapBuiltInParserHandler.js';
import {
  ToCssCustomPropertiesParserDefinition,
  toCssCustomPropertiesParserDefinition,
} from './definition.js';
import { toCssCustomPropertiesHandler } from './toCssCustomPropertiesHandler.js';

/* v8 ignore start */
export const toCssCustomProperties: DeriveMakeParserFunctionFromDefinition<
  ToCssCustomPropertiesParserDefinition
> =
  ({ options, output }) =>
  (dataBox, toolbox) =>
    wrapBuiltInParserHandler(toCssCustomPropertiesParserDefinition, toCssCustomPropertiesHandler, {
      dataBox,
      toolbox,
      options,
      output,
    });

/* v8 ignore stop */
