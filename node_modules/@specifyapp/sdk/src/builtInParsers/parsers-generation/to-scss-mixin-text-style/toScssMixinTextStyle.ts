import { DeriveMakeParserFunctionFromDefinition } from '../../internals/createBuiltInParserDefinition.js';
import { wrapBuiltInParserHandler } from '../../internals/wrapBuiltInParserHandler.js';
import {
  toScssMixinTextStyleParserDefinition,
  ToScssMixinTextStyleParserDefinition,
} from './definition.js';
import { toScssMixinTextStyleHandler } from './toScssMixinTextStyleHandler.js';

/* v8 ignore start */
export const toScssMixinTextStyle: DeriveMakeParserFunctionFromDefinition<
  ToScssMixinTextStyleParserDefinition
> =
  ({ options, output }) =>
  (dataBox, toolbox) =>
    wrapBuiltInParserHandler(toScssMixinTextStyleParserDefinition, toScssMixinTextStyleHandler, {
      dataBox,
      toolbox,
      options,
      output,
    });
/* v8 ignore stop */
