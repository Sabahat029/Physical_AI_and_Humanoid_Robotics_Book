import { DeriveMakeParserFunctionFromDefinition } from '../../internals/createBuiltInParserDefinition.js';
import { wrapBuiltInParserHandler } from '../../internals/wrapBuiltInParserHandler.js';
import { toJsonListParserDefinition, ToJsonListParserDefinition } from './definition.js';
import { toJsonListHandler } from './toJsonListHandler.js';

/* v8 ignore start */
export const toJsonList: DeriveMakeParserFunctionFromDefinition<ToJsonListParserDefinition> =
  ({ options, output }) =>
  (dataBox, toolbox) =>
    wrapBuiltInParserHandler(toJsonListParserDefinition, toJsonListHandler, {
      dataBox,
      toolbox,
      options,
      output,
    });

/* v8 ignore stop */
