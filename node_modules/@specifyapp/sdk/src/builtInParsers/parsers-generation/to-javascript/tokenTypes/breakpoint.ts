import { SpecifyBreakpointValue } from '@specifyapp/specify-design-token-format';

export function renderBreakpoint(breakpoint: SpecifyBreakpointValue) {
  return `${breakpoint.value}${breakpoint.unit}`;
}
