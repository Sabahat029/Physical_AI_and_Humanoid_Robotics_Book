import { DeriveMakeParserFunctionFromDefinition } from '../../internals/createBuiltInParserDefinition.js';
import { wrapBuiltInParserHandler } from '../../internals/wrapBuiltInParserHandler.js';
import { toJavascriptParserDefinition, ToJavascriptParserDefinition } from './definition.js';
import { toJavascriptHandler } from './toJavascriptHandler.js';

/* v8 ignore start */
export const toJavascript: DeriveMakeParserFunctionFromDefinition<ToJavascriptParserDefinition> =
  ({ options, output }) =>
  (dataBox, toolbox) =>
    wrapBuiltInParserHandler(toJavascriptParserDefinition, toJavascriptHandler, {
      dataBox,
      toolbox,
      options,
      output,
    });
/* v8 ignore stop */
