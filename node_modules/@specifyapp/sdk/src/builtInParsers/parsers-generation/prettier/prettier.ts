import { DeriveMakeParserFunctionFromDefinition } from '../../internals/createBuiltInParserDefinition.js';
import { wrapBuiltInParserHandler } from '../../internals/wrapBuiltInParserHandler.js';
import { prettierParserDefinition, PrettierParserDefinition } from './definition.js';
import { prettierHandler } from './prettierHandler.js';

export const prettier: DeriveMakeParserFunctionFromDefinition<PrettierParserDefinition> =
  ({ options, output }) =>
  (dataBox, toolbox) =>
    wrapBuiltInParserHandler(prettierParserDefinition, prettierHandler, {
      dataBox,
      toolbox,
      options,
      output,
    });
