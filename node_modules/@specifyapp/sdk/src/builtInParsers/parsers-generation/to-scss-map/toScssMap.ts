import { DeriveMakeParserFunctionFromDefinition } from '../../internals/createBuiltInParserDefinition.js';
import { wrapBuiltInParserHandler } from '../../internals/wrapBuiltInParserHandler.js';
import { toScssMapParserDefinition, ToScssMapParserDefinition } from './definition.js';
import { toScssMapHandler } from './toScssMapHandler.js';

/* v8 ignore start */
export const toScssMap: DeriveMakeParserFunctionFromDefinition<ToScssMapParserDefinition> =
  ({ options, output }) =>
  (dataBox, toolbox) =>
    wrapBuiltInParserHandler(toScssMapParserDefinition, toScssMapHandler, {
      dataBox,
      toolbox,
      options,
      output,
    });
/* v8 ignore stop */
