export const convertToFontFaces = (outputs: Array<{ value: Record<string, unknown> }>): string => {
  return outputs
    .map(output => {
      return `@font-face {
${Object.entries(output.value)
  .map(([key, value]) => `\t${key}: ${value};`)
  .join('\n')}
}`;
    })
    .join('\n');
};
