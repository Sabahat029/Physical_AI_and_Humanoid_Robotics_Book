import { DeriveMakeParserFunctionFromDefinition } from '../../internals/createBuiltInParserDefinition.js';
import { wrapBuiltInParserHandler } from '../../internals/wrapBuiltInParserHandler.js';
import { toSwiftParserDefinition, ToSwiftParserDefinition } from './definition.js';
import { toSwiftHandler } from './toSwiftHandler.js';

/* v8 ignore start */
export const toSwift: DeriveMakeParserFunctionFromDefinition<ToSwiftParserDefinition> =
  ({ options, output }) =>
  (dataBox, toolbox) =>
    wrapBuiltInParserHandler(toSwiftParserDefinition, toSwiftHandler, {
      dataBox,
      toolbox,
      options,
      output,
    });
/* v8 ignore stop */
