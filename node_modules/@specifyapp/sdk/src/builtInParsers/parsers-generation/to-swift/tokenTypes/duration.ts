import { SpecifyDurationValue } from '@specifyapp/specify-design-token-format';

export function durationToSwift(duration: SpecifyDurationValue) {
  // Durations are in seconds in Swift
  return (duration.unit === 's' ? duration.value : duration.value / 1000).toString();
}
