import { SpecifyCubicBezierValue } from '@specifyapp/specify-design-token-format';
import { numberToDouble } from '../utils.js';

export function cubicBezierToSwift(timingFunction: SpecifyCubicBezierValue) {
  return `Animation.timingCurve(${timingFunction.map(numberToDouble).join(', ')})`;
}
