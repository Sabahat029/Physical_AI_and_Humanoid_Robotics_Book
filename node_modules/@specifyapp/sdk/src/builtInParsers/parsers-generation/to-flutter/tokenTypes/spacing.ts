import { SpecifySpacingValue } from '@specifyapp/specify-design-token-format';
import { numberToDouble } from '../utils.js';

export function renderSpacing(spacing: SpecifySpacingValue) {
  return `EdgeInsets.all(${numberToDouble(spacing.value)})`;
}
