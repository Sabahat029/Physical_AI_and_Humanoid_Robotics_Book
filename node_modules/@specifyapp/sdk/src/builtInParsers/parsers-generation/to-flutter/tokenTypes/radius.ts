import { SpecifyRadiusValue } from '@specifyapp/specify-design-token-format';
import { numberToDouble } from '../utils.js';

export function renderRadius(radius: SpecifyRadiusValue) {
  return `Radius.circular(${numberToDouble(radius.value)})`;
}
