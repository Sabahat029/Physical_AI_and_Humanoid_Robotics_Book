import { SpecifyCubicBezierValue } from '@specifyapp/specify-design-token-format';
import { numberToDouble } from '../utils.js';

export function renderCubicBezier(timingFunction: SpecifyCubicBezierValue) {
  return `Cubic(${timingFunction.map(numberToDouble).join(', ')})`;
}
