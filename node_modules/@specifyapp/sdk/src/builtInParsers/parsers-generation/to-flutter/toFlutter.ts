import { DeriveMakeParserFunctionFromDefinition } from '../../internals/createBuiltInParserDefinition.js';
import { wrapBuiltInParserHandler } from '../../internals/wrapBuiltInParserHandler.js';
import { toFlutterParserDefinition, ToFlutterParserDefinition } from './definition.js';
import { toFlutterHandler } from './toFlutterHandler.js';

/* v8 ignore start */
export const toFlutter: DeriveMakeParserFunctionFromDefinition<ToFlutterParserDefinition> =
  ({ options, output }) =>
  (dataBox, toolbox) =>
    wrapBuiltInParserHandler(toFlutterParserDefinition, toFlutterHandler, {
      dataBox,
      toolbox,
      options,
      output,
    });
/* v8 ignore stop */
