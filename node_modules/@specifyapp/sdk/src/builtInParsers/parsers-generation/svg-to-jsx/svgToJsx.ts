import { DeriveMakeParserFunctionFromDefinition } from '../../internals/createBuiltInParserDefinition.js';
import { wrapBuiltInParserHandler } from '../../internals/wrapBuiltInParserHandler.js';
import { svgToJsxParserDefinition, SvgToJsxParserDefinition } from './definition.js';
import { svgToJsxHandler } from './svgToJsxHandler.js';

/* v8 ignore start */
export const svgToJsx: DeriveMakeParserFunctionFromDefinition<SvgToJsxParserDefinition> =
  ({ options, output }) =>
  (dataBox, toolbox) =>
    wrapBuiltInParserHandler(svgToJsxParserDefinition, svgToJsxHandler, {
      dataBox,
      toolbox,
      options,
      output,
    });

/* v8 ignore stop */
