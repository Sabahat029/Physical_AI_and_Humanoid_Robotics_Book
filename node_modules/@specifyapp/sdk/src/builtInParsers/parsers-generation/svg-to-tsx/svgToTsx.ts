import { DeriveMakeParserFunctionFromDefinition } from '../../internals/createBuiltInParserDefinition.js';
import { wrapBuiltInParserHandler } from '../../internals/wrapBuiltInParserHandler.js';
import { svgToTsxParserDefinition, SvgToTsxParserDefinition } from './definition.js';
import { svgToTsxHandler } from './svgToTsxHandler.js';

/* v8 ignore start */
export const svgToTsx: DeriveMakeParserFunctionFromDefinition<SvgToTsxParserDefinition> =
  ({ options, output }) =>
  (dataBox, toolbox) =>
    wrapBuiltInParserHandler(svgToTsxParserDefinition, svgToTsxHandler, {
      dataBox,
      toolbox,
      options,
      output,
    });

/* v8 ignore stop */
