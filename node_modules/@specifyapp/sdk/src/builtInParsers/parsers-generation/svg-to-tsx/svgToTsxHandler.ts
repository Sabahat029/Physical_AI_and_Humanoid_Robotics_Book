import { DeriveBuiltInParserHandlerFromDefinition } from '../../internals/createBuiltInParserDefinition.js';
import type { SvgToJsxParserDefinition } from '../svg-to-jsx/definition.js';
import { svgToJsxHandler } from '../svg-to-jsx/svgToJsxHandler.js';

export const svgToTsxHandler: DeriveBuiltInParserHandlerFromDefinition<SvgToJsxParserDefinition> = (
  previousDataBox,
  toolbox,
  parserOptions,
  outputOptions,
  context,
) => {
  return svgToJsxHandler(
    previousDataBox,
    toolbox,
    { tsx: true, ...(parserOptions ?? {}) },
    outputOptions,
    context,
  );
};
