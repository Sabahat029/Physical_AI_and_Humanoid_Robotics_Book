import { SpecifyRadiiValue } from '@specifyapp/specify-design-token-format';
import { dimensionToJson } from './dimension.js';

export function radiiToJson(value: SpecifyRadiiValue) {
  return value.map(dimensionToJson).join(' ');
}
