import { SpecifySpacingsValue } from '@specifyapp/specify-design-token-format';
import { dimensionToJson } from './dimension.js';

export function spacingsToJson(value: SpecifySpacingsValue) {
  return value.map(dimensionToJson).join(' ');
}
