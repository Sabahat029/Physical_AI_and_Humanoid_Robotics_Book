import { DeriveMakeParserFunctionFromDefinition } from '../../internals/createBuiltInParserDefinition.js';
import { wrapBuiltInParserHandler } from '../../internals/wrapBuiltInParserHandler.js';
import { toJsonParserDefinition, ToJsonParserDefinition } from './definition.js';
import { toJsonHandler } from './toJsonHandler.js';

/* v8 ignore start */
export const toJson: DeriveMakeParserFunctionFromDefinition<ToJsonParserDefinition> =
  ({ options, output }) =>
  (dataBox, toolbox) =>
    wrapBuiltInParserHandler(toJsonParserDefinition, toJsonHandler, {
      dataBox,
      toolbox,
      options,
      output,
    });
/* v8 ignore stop */
