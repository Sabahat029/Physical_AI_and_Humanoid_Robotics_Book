import { DeriveMakeParserFunctionFromDefinition } from '../../internals/createBuiltInParserDefinition.js';
import { wrapBuiltInParserHandler } from '../../internals/wrapBuiltInParserHandler.js';
import { toBitmapFileParserDefinition, ToBitmapFileParserDefinition } from './definition.js';
import { toBitmapFileHandler } from './toBitmapFileHandler.js';

/* v8 ignore start */
export const toBitmapFile: DeriveMakeParserFunctionFromDefinition<ToBitmapFileParserDefinition> =
  ({ options, output }) =>
  (dataBox, toolbox) =>
    wrapBuiltInParserHandler(toBitmapFileParserDefinition, toBitmapFileHandler, {
      dataBox,
      toolbox,
      options,
      output,
    });

/* v8 ignore stop */
