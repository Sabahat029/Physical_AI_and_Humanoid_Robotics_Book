import { DeriveMakeParserFunctionFromDefinition } from '../../internals/createBuiltInParserDefinition.js';
import { wrapBuiltInParserHandler } from '../../internals/wrapBuiltInParserHandler.js';
import { svgoParserDefinition, SvgoParserDefinition, svgoParserName } from './definition.js';
import { svgoHandler } from './svgoHandler.js';

/* v8 ignore start */
export const svgo: DeriveMakeParserFunctionFromDefinition<SvgoParserDefinition> =
  ({ options, output }) =>
  (dataBox, toolbox) =>
    wrapBuiltInParserHandler(svgoParserDefinition, svgoHandler, {
      dataBox,
      toolbox,
      options,
      output,
    });
/* v8 ignore stop */
