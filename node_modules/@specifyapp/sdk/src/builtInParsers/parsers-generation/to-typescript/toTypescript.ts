import { DeriveMakeParserFunctionFromDefinition } from '../../internals/createBuiltInParserDefinition.js';
import { wrapBuiltInParserHandler } from '../../internals/wrapBuiltInParserHandler.js';
import { toTypescriptParserDefinition, ToTypescriptParserDefinition } from './definition.js';
import { toTypescriptHandler } from './toTypescriptHandler.js';

/* v8 ignore start */
export const toTypescript: DeriveMakeParserFunctionFromDefinition<ToTypescriptParserDefinition> =
  ({ options, output }) =>
  (dataBox, toolbox) =>
    wrapBuiltInParserHandler(toTypescriptParserDefinition, toTypescriptHandler, {
      dataBox,
      toolbox,
      options,
      output,
    });
/* v8 ignore stop */
