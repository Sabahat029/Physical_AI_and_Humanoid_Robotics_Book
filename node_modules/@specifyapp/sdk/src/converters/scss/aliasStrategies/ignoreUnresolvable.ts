import { ScssUnresolvableAliasStrategy } from './ScssAliasStrategy.js';

export function createIgnoreUnresolvableStrategy() {
  const strategy = ((_tokenState, _alias) => {
    return undefined;
  }) as ScssUnresolvableAliasStrategy<undefined>;

  return strategy;
}
