import { CssUnresolvableAliasStrategy } from './CssAliasStrategy.js';

export function createIgnoreUnresolvableStrategy() {
  const strategy = ((_tokenState, _alias) => {
    return undefined;
  }) as CssUnresolvableAliasStrategy<undefined>;

  return strategy;
}
