import { z } from 'zod';
import { specifyModeAndValueLevelAliasSignatureSchema, } from './designTokenAlias.js';
// makeUnionWithModeAndValueLevelAliasValue is defined to help TS map out the adequate return types
export function makeUnionWithModeAndValueLevelAliasValue(schema) {
    function computeAliasingSupport(isSupportingAliasing) {
        return isSupportingAliasing
            ? z.union([schema, specifyModeAndValueLevelAliasSignatureSchema])
            : schema;
    }
    return computeAliasingSupport;
}
