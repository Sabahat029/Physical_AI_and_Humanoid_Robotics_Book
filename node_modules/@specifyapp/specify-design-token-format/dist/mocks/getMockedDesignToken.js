import { getMockedDesignTokenValue } from './getMockedDesignTokenValue.js';
export function getMockedDesignToken(options) {
    const { type, value, extensions, description } = options;
    const $value = value ?? { default: getMockedDesignTokenValue(type) };
    return {
        $type: type,
        $value,
        /* v8 ignore start */
        ...(description ? { $description: description } : {}),
        ...(extensions ? { $extensions: extensions } : {}),
        /* v8 ignore stop */
    };
}
