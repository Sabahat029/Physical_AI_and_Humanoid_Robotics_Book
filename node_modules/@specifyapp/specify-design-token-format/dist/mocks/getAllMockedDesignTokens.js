import { specifyDesignTokenTypeNames, } from '../definitions/index.js';
import { getMockedDesignToken } from './getMockedDesignToken.js';
export function getAllMockedDesignTokens(options = {
    asArray: false,
}) {
    const tokens = specifyDesignTokenTypeNames.map(typeName => getMockedDesignToken({ type: typeName }));
    if (options.asArray) {
        return tokens;
    }
    return tokens.reduce((acc, token) => ({
        ...acc,
        [token.$type]: token,
    }), {});
}
