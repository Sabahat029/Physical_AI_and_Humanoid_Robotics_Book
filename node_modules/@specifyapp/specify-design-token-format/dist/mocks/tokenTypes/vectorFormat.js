import { pickRandomInList } from '../../utils/mockGenerators.js';
import { specifyVectorFormatValues, } from '../../definitions/tokenTypes/vectorFormat.js';
export function getMockedVectorFormatValue(override) {
    if (override) {
        return override;
    }
    return pickRandomInList(specifyVectorFormatValues);
}
