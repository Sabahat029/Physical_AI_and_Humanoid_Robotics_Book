import { pickRandomInList } from '../../utils/mockGenerators.js';
import { specifyBitmapFormatValues, } from '../../definitions/tokenTypes/bitmapFormat.js';
export function getMockedBitmapFormatValue(override) {
    if (override) {
        return override;
    }
    return pickRandomInList(specifyBitmapFormatValues);
}
