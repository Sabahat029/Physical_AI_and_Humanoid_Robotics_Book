import { pickRandomInList } from '../../utils/mockGenerators.js';
import { specifyDimensionUnitValues, } from '../../definitions/tokenTypes/dimensionUnit.js';
export function getMockedDimensionUnitValue(selector) {
    if (selector) {
        return selector;
    }
    return pickRandomInList(specifyDimensionUnitValues);
}
