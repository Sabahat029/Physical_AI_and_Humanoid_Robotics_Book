import { pickRandomInList } from '../../utils/mockGenerators.js';
import { specifyShadowTypeTypeValues, } from '../../definitions/tokenTypes/shadowType.js';
export function getMockedShadowTypeValue(override) {
    return override ?? pickRandomInList(specifyShadowTypeTypeValues);
}
