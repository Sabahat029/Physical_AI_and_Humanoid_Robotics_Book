import { pickRandomNumberInRange } from '../../utils/mockGenerators.js';
export function getMockedCubicBezierValue(override) {
    return (override ?? [
        Math.random(),
        pickRandomNumberInRange(-3, 5),
        Math.random(),
        pickRandomNumberInRange(-3, 5),
    ]);
}
