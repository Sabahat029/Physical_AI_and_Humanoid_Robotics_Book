import { TreeNodeState } from './TreeNodeState.js';
/**
 * @deprecated
 */
export class UnresolvableTokenState extends TreeNodeState {
    reason;
    constructor(treeState, path, reason) {
        super(treeState, {
            name: path.tail(),
            path,
            $description: undefined,
            $extensions: undefined,
        });
        this.reason = reason;
    }
    getCommonJSON() {
        return {
            $alias: this.path.toString(),
        };
    }
    toJSON() {
        return null;
    }
}
